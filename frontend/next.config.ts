import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.gr-assets.com'], // Add the hostname of your external images
  },
};

export default nextConfig;
